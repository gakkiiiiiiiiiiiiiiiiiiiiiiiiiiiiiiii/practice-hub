# API 配置
# API 基础地址（完整 URL，包含协议；末尾可有 /api 也可只有域名）
# 本地开发：http://127.0.0.1:3333/api
# 微信云托管：如 https://xxx.sh.run.tcloudbase.com（小程序 uploadFile 必须 HTTPS，构建时会写入）
# 二选一即可：优先读 VITE_API_BASE_URL，未设置时 vite 会读 API_BASE_URL
# VITE_API_BASE_URL=https://你的服务.sh.run.tcloudbase.com
# API_BASE_URL=https://你的服务.sh.run.tcloudbase.com
VITE_API_BASE_URL=http://127.0.0.1:3333/api

# uni-app 源码目录（本项目页面在 font-end 根目录，不在 src/）
UNI_INPUT_DIR=.
